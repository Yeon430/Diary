# Builder.io dev-tools 실행 후 가이드

## 1단계: 명령어 실행 확인

명령어를 실행하면 Builder.io가 디자인을 분석하고 코드를 생성합니다. 
터미널에서 다음 메시지들을 확인하세요:
- ✅ 코드 생성 완료
- 📁 생성된 파일 경로
- ⚠️ 필요한 의존성 목록

## 2단계: 생성된 파일 확인

명령어 실행 후 다음 위치에서 생성된 파일들을 확인하세요:

```
프로젝트 루트/
├── components/          # 생성된 컴포넌트들
│   ├── *.tsx
│   └── *.ts
├── styles/             # 스타일 파일들 (있는 경우)
└── ...
```

또는 명령어가 특정 디렉토리를 지정했을 수 있습니다. 터미널 출력을 확인하세요.

## 3단계: 의존성 설치

생성된 코드에 필요한 패키지가 있다면 설치합니다:

```bash
npm install
```

터미널에 추가 패키지 설치가 필요하다고 나오면 해당 패키지들을 설치하세요.

## 4단계: 생성된 컴포넌트 사용하기

생성된 컴포넌트를 프로젝트에 통합합니다.

### 예시 1: app/page.tsx에서 사용

```tsx
import YourComponent from '@/components/YourComponent'

export default function Home() {
  return (
    <main>
      <YourComponent />
    </main>
  )
}
```

### 예시 2: 새 페이지 생성

```tsx
// app/your-page/page.tsx
import YourComponent from '@/components/YourComponent'

export default function YourPage() {
  return <YourComponent />
}
```

## 5단계: 개발 서버 실행 및 확인

```bash
npm run dev
```

브라우저에서 `http://localhost:3000`을 열어 생성된 컴포넌트가 제대로 표시되는지 확인하세요.

## 6단계: 코드 검토 및 수정

생성된 코드를 검토하고 필요에 따라 수정:

- ✅ 스타일 조정
- ✅ 기능 추가/수정
- ✅ 타입 에러 수정
- ✅ 성능 최적화

## 문제 해결

### 컴포넌트를 찾을 수 없을 때
- 생성된 파일 경로를 확인하세요
- import 경로가 올바른지 확인하세요

### 타입 에러가 발생할 때
```bash
npm install --save-dev @types/react @types/react-dom
```

### 스타일이 적용되지 않을 때
- CSS 파일이 제대로 import 되었는지 확인
- Tailwind CSS를 사용한다면 설정 확인

## 추가 리소스

- [Builder.io 문서](https://www.builder.io/c/docs)
- [Next.js 문서](https://nextjs.org/docs)


